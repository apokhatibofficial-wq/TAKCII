# نشر TAK-C.TAXI (PWA)

> **تحديث**: المشروع الآن تطبيق حقيقي (`apps/rider` + Supabase) بدل النموذج الأولي
> أسفله. خطوات النشر والدومين الكاملة تُكتب في المرحلة السابعة؛ قسم «حسابات
> التجربة» أدناه محدَّث ليعكس الحسابات الحقيقية على القاعدة الحية. بقية هذا
> الملف يصف النموذج الأولي (`index.html` وما حوله) كمرجع تاريخي.

## الملفات المطلوبة على السيرفر
ارفع محتوى المجلد كما هو، بنفس الأسماء والمسارات:

```
index.html                  ← التطبيق (نسخة TAK-C Taxi.dc.html)
support.js
osm.js
sw.js
manifest.webmanifest
icon-192.png  icon-512.png  apple-touch-icon.png
assets/       ← الشعار والخط وأيقونة العدّاد
```

## خطوات النشر
1. أنشئ مستودعاً على GitHub وارفع الملفات.
2. Settings ← Pages ← Branch: `main` / `root` ← Save.
   (أو استخدم Netlify / Cloudflare Pages: اسحب المجلد فقط.)
3. افتح الرابط `https://<اسمك>.github.io/<المستودع>/`.

**HTTPS إلزامي** — بدونه لا يعمل تحديد الموقع ولا التثبيت على الشاشة الرئيسية.

## التثبيت على الهاتف
- أندرويد/كروم: رسالة «إضافة إلى الشاشة الرئيسية» تظهر تلقائياً.
- آيفون/سفاري: مشاركة ← «إضافة إلى الشاشة الرئيسية».

## التحديثات
بعد أي تعديل ارفع الملفات فقط، وارفع رقم `VERSION` في `sw.js`
(مثال: `takc-v1` ← `takc-v2`). كل من ثبّت التطبيق يستلم النسخة
الجديدة تلقائياً دون إعادة تثبيت أو حذف.

## حسابات التجربة (على القاعدة الحقيقية الآن)
كلمة المرور الحرفية `1234` من النموذج لا تجتاز حد Supabase Auth الأدنى
(6 محارف)، فكلمة مرور كل حسابات التجربة أصبحت **`takc1234`**:
- راكب: `mohamad` / `takc1234` (وأيضاً `sara`، وحساب موقوف `baraa` لتجربة رسالة «الحساب موقوف»)
- سائق: `ahmad` / `takc1234` (نشط ومتصل) — وأيضاً `khaled` (نشط ومتصل)، `omar` (نشط وغير متصل)، `yaser` (قيد المراجعة)
- أدمن: حساب `admin` موجود على القاعدة (`admin@tak-c.taxi`)؛ كلمة المرور لم تُوثَّق هنا — إن لزم إعادة تعيينها فذلك من لوحة Supabase مباشرة.

## تطبيق السائق (APK — المرحلة الخامسة)
`apps/driver` تطبيق React Native/Expo منفصل تماماً عن الـ PWA، لأن تتبع
الـ GPS في الخلفية (أثناء إغلاق الشاشة) غير ممكن بشكل موثوق داخل PWA. يُوزَّع
كملف APK يُثبَّته السائق يدوياً (sideload) لا عبر متجر Play.

**بناء نسخة تجريبية (debug APK):**
```
cd apps/driver
npx expo prebuild --platform android   # يولّد مجلد android/ (غير مرفوع على git)
cd android && ./gradlew assembleDebug
# الناتج: android/app/build/outputs/apk/debug/app-debug.apk
```
هذا توقيع تجريبي (debug signing) يكفي للتثبيت اليدوي والتجربة الآن؛ التوزيع
الفعلي لاحقاً يحتاج مفتاح توقيع إنتاجي (release keystore) منفصل — لم يُنشأ بعد.

- تسجيل السائق الذاتي لا يمر بكود تحقق (الأدمن يراجع يدوياً)، لذلك الحساب
  يُنشأ مباشرة عبر Edge Function باسم `driver-signup` (وليس `auth.signUp`
  العادي من التطبيق) لأن `mailer_autoconfirm` مطفأ على المشروع (يلزم فعّالاً
  لتحقق OTP الحقيقي للركاب)، وبدونه لن يحصل السائق على جلسة بعد التسجيل.
- خلفية الموقع تعمل فقط في بناء native حقيقي (وليس Expo Go)، وتحتاج قبول
  صلاحيتي الموقع الأمامي والخلفي من السائق عند أول تفعيل للاتصال.

## ملاحظات تشغيلية
- خريطة الشوارع والمسافات تُسحب من OpenStreetMap/OSRM عند أول فتح
  وتُحدَّث أسبوعياً، وتعمل بعدها دون إنترنت.
- البيانات الآن محلية على الجهاز (localStorage) بطبقة واحدة معزولة
  (`loadDb/updateDb/loadLive`) — استبدالها بـ API أو Firebase لاحقاً
  لا يمسّ الواجهات.
