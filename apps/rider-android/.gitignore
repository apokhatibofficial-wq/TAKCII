# Everything here is regenerated by `bubblewrap build`/`update` from
# twa-manifest.json (and the live web manifest/icons it points at) -- only
# the manifest and the signing key are real source.
.gradle/
app/
gradle/
build.gradle
gradlew
gradlew.bat
settings.gradle
gradle.properties
store_icon.png
manifest-checksum.txt
*.apk
*.aab
